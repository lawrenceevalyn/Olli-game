from nose.tools import *
from gameweb import lexicon


def test_engine():
# test that N, S, E, W correctly move the player to the next room
# test that if there's no room it says "You can't go that way."

def text_inputs():
# some kind of test to confirm the file processes inputs properly?
# right now the program doesn't really parse inputs, but later it will
# (and at that time I will want to test that it hands things off to the
# parser correctly)